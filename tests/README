The tokenizer testcases require the use of simplejson
(http://cheeseshop.python.org/pypi/simplejson). 
There are three ways to run the tests: 

All the tests: ./runtests.py 
Just the tokenizer tests: python test_tokenizer.py 
Just the parser tests: python test_parser.py